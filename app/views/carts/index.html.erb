<p id="notice"><%= notice %></p>

<h1>未完成订单</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @carts.each do |cart| %>
      <tr>
        <td>订单<%= cart.id %></td>
        <td><%= link_to '查看', cart %></td>
        <td><%= link_to '删除', cart, method: :delete , data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

